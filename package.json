{
  "name": "bd-analytics-dashboard",
  "version": "1.0.0",
  "description": "Unified BD Analytics Dashboard MERN Stack App",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build-client": "npm run build --prefix client",
    "build": "npm install --prefix server && NODE_ENV=development npm install --prefix client && npm run build --prefix client",
    "start": "node server/server.js"
  }
}
